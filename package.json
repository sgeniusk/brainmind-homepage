{
  "name": "homepage-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "sync:status": "node scripts/sync-project-state.mjs",
    "check:project": "node scripts/check-project-state.mjs",
    "check:a11y": "node scripts/check-accessibility-contracts.mjs",
    "check:harness": "node scripts/check-harness-contracts.mjs",
    "check:ux": "node scripts/check-homepage-ux.mjs",
    "verify": "npm run sync:status && npm run build && node scripts/check-blog-integrity.mjs && npm run check:project && npm run check:a11y && npm run check:harness && npm run check:ux",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.2",
    "astro": "^6.0.8"
  }
}
